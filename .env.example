# Google Apps Script URL for Sheets integration
VITE_APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# Optional: override backend API URL (defaults to same origin)
VITE_API_URL=

# Google Gemini API key for AI image-to-data extraction.
# Get your key at https://aistudio.google.com/app/apikey
#
# Server deployment (Cloud Run, VPS, etc.):
#   Set GEMINI_API_KEY as a server-side environment variable.
#   The backend proxy at /api/gemini will use it automatically.
GEMINI_API_KEY=your_gemini_api_key_here

# Static deployment (GitHub Pages):
#   Set VITE_GEMINI_API_KEY as a GitHub secret so it is embedded at build time.
VITE_GEMINI_API_KEY=
