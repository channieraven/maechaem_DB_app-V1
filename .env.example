# Google Apps Script URL for Sheets integration
VITE_APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# Optional: override backend API base URL (defaults to same origin).
# Set this as a GitHub repository *variable* when the frontend is hosted on
# GitHub Pages and the backend runs on a separate server (e.g. Cloud Run).
VITE_GEMINI_API_URL=

# --- AI configuration (at least one is required for file-import OCR) ---

# Google Gemini API key — supports PDF and images.
# Get your key for free at https://aistudio.google.com/app/apikey
#
# Server deployment (Cloud Run, VPS, etc.):
#   Set GEMINI_API_KEY as a server-side environment variable.
#   The backend proxy at /api/gemini will use it automatically.
GEMINI_API_KEY=your_gemini_api_key_here

# Static deployment (GitHub Pages):
#   Set VITE_GEMINI_API_KEY as a GitHub Actions secret so it is embedded at build time.
VITE_GEMINI_API_KEY=

# OpenAI API key — alternative to Gemini, supports images only (not PDF).
# Get your key at https://platform.openai.com/api-keys
#
# Server deployment: set OPENAI_API_KEY as a server-side environment variable.
OPENAI_API_KEY=your_openai_api_key_here

# Static deployment (GitHub Pages): set as a GitHub Actions secret.
VITE_OPENAI_API_KEY=
